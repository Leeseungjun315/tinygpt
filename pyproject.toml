[project]
name = "tinygpt"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "ollama>=0.4.0",
  "rich>=13.0.0",
]

[project.scripts]
tinygpt = "tinygpt.main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]